// Main theme configuration - combines all theme tokens
import colors from './colors';
import typography from './typography';
import { spacing, borderRadius, shadows, layout } from './layout';

const theme = {
    colors,
    typography,
    spacing,
    borderRadius,
    shadows,
    layout,
};

export default theme;
